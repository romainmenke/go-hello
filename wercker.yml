box: golang

build:
  steps:
    - setup-go-workspace

    - script:
        name: go build
        code: |
          go build
    - script:
        name: go test
        code: |
          go test
  # after-steps:
  #   - slack-notifier:
  #       url: $SLACK_URL
  #       channel: cfda
  #       notify_on: failed
